var cars = ['volvo', 'saab', 'ford', 'bmw', 'fiat'];

var i = 1;
var numberOfTimes = 6;

// will run 5 times
while (i < cars.length) {
  // code block to be executed
  console.log('i is ' + i);
  i++;
}