let cars = ['volvo', 'saab', 'ford', 'bmw', 'fiat'];

let i = 0;
let numberOfTimes = 6;

// will run 5 times
while (i < cars.length) {
  // code block to be executed
  console.log('i is ', i);
  console.log('car', cars[i])
  i++;
}