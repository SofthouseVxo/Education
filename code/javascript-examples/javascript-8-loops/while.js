var cars = ['volvo', 'saab', 'ford', 'bmw', 'fiat'];

var i = 0;
var numberOfTimes = 6;

// will run 5 times
while (i < cars.length) {
  // code block to be executed
  console.log('i is ' + cars[i]);
  i++;
}